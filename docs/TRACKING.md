# Shared tracking operation

1. Open the hosted site while signed in as the consultant.
2. Create a client project. The creation dialog can carry forward the currently open legacy local answers, or start empty. No old local backup is deleted.
3. In Project tracking, update the public summary. Expand each Jira-derived task to record status, actual hours and a client-visible note. Use Out of scope for work not included. The uncertain VIP and truncated trigger items stay explicitly marked as requiring clarification.
4. In Meetings, fill a date/topic and check Held after each meeting. Exactly eight records exist, each 30 minutes. Scheduled but unchecked meetings do not consume the count. Logged configuration hours are separate.
5. The agent and administrator course links are already in the catalog. Their access codes are managed as server secrets. In Training, maintain the bilingual shared catalog with HTTPS links. Enable Training included and select resources for each project. Disable to hide the section and exclude URLs from actual client responses.
6. Authorize the client's email inside that project, then separately share the private Site using platform controls. No invitation is automatically sent. The client will see only projects associated with their authenticated email.
7. Refresh when necessary; idle screens check every 30 seconds. If a conflict appears, export your unsaved JSON before loading the current version. Do not discard another collaborator's updates.
8. Export JSON for full project responses/tracking, and XLSX for questionnaire, implementation, meetings and currently included training links. The shared catalog is a separate server record; a project JSON backup contains selected resource IDs, not the global catalog.

Current source: Jira screenshots KAN-548–KAN-574. The corresponding Jira board is not connected. Updates are entered by the consultant, never inferred from live Zendesk or Jira.

In Company, enter the Zendesk URL and account owner email. Prefer a named account for Stephanie. If a temporary handover is needed, use its dedicated encrypted form; do not paste passwords into notes. Retrieval is one-time and owner-only. Course codes gate the portal link, not the original public destination pages.
